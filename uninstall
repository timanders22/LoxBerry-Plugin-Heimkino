#!/bin/sh
# Der Dienst wird beendet. Systempakete bleiben stehen: python3-cryptography
# und python3-requests werden von anderen Dingen mitbenutzt, ein blindes
# Entfernen waere schaedlich.
pkill -f "hk_service.py" >/dev/null 2>&1
exit 0
